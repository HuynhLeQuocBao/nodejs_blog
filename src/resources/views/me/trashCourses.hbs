<div class="mt-4">
  <div>
      <h3>Khóa học đã xóa</h3>
      <a href="/me/stored/courses">Danh sách khóa học</a>
  </div>

  <table class="table mt-4">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Tên khóa học</th>
        <th scope="col">Mô tả</th>
        <th scope="col">Thời gian xóa</th>
        <th scope="col" colspan="2" style="text-align: center;">Thao tác</th>
      </tr>
    </thead>
    <tbody>
      {{#each courses}}
      <tr>
        <th scope="row">{{sum @index 1}}</th>
        <td scope="row">{{this.name}}</td>
        <td scope="row">{{this.description}}</td>
        <td scope="row">@{{this.deletedAt}}</td>
        <td scope="row">
          <form method="post" action="/courses/{{this._id}}/restore?_method=PATCH">
            <input type="submit" class="btn btn-primary" value="Khôi phục">
          </form>
        </td>
        <td scope="row">
          <a href="/courses/{{this._id}}" class="btn btn-danger" data-id="{{this._id}}" data-toggle="modal" data-target="#delete-course-modal">Xóa vĩnh viễn</a>
        </td>
      </tr>
      <div id="delete-course-modal" class="modal" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title">Xóa khóa học vĩnh viễn</h5>
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>
            </div>
            <div class="modal-body">
              <p>Hành động này sẽ không thể khôi phục. Bạn có chắc chắn muốn xóa vĩnh viễn khóa học này?</p>
            </div>
            <div class="modal-footer">
              <form method="post" name="delete-course-form" action="/courses/{{this._id}}/force?_method=DELETE">
                <button id="btn-delete-course" type="submit" class="btn btn-danger">Xóa</button>
              </form>
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Hủy</button>
            </div>
          </div>
        </div>
      </div>
      {{else}}
      <tr>
        <td colspan="5" class="text-center">
          Thùng rác trống.
          <a href="/me/stored/courses">Danh sách khóa học</a>
        </td>
      </tr>
      {{/each}}
    </tbody>
</table>
</div>

{{!-- <script>
  document.addEventListener('DOMContentLoaded', function () {

    var courseId;
    var deleteForm = document.forms['delete-course-form'];
    var deleteModal = document.getElementById('delete-course-modal')

    deleteModal.addEventListener('show.bs.modal', function (event) {

    courseId = event.relatedTarget.getAttribute('data-id')

    var btnDelCourse = document.querySelector('.btn-delete-course')
      btnDelCourse.onclick = () => {
        deleteForm.action = `/courses/${courseId}?_method=DELETE`
        deleteForm.submit()
      }
    })
  })
</script> --}}